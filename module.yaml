product:
  # NOTE: currently only packaged for x86-64 Linux (linuxX64 in Kotlin-Toolchain)
  type: linux/app 
  platforms: [linuxX64]

dependencies:
  - com.github.ajalt.clikt:clikt:5.1.0 # CLI parsing 
