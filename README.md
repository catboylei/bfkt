# bfkt
A (fancy) Brainfuck Toolchain

> [!IMPORTANT]
>
> New project, obviously WIP
> to contrib dm @catboylei

> [!TIP] 
> **TODOS**
> LLVM compiler to native linux binaries 
> Interpreter with --nocompile flag 
> All optimisations e.g. folding, known patterns
> Various compiler flags for cell width, wrapping, showcells, etc 
> --runinfo (instruction count, time)
> --jsonoutput (populated with returns from other flags)
> Step debugger TUI
